services:
  db:
    image: pgvector/pgvector:pg16
    environment:
      POSTGRES_USER: plainsheet
      POSTGRES_PASSWORD: plainsheet
      POSTGRES_DB: plainsheet
    ports:
      - "5433:5432"
    volumes:
      - plainsheet-pgdata:/var/lib/postgresql/data
volumes:
  plainsheet-pgdata:
